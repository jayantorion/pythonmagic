# AI Job Search & Application Intelligence Platform - Python Dependencies
# Install with: pip install -r requirements.txt
# Tested with Python 3.11, 3.12, 3.13

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Data validation & settings
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database (dual-mode: SQLite local + PostgreSQL production)
sqlalchemy>=2.0.30
aiosqlite>=0.20.0          # SQLite async driver (default local mode)
asyncpg>=0.29.0            # PostgreSQL async driver (production mode)

# AI / LLM integration
anthropic>=0.34.0          # Anthropic Claude 3.5

# HTTP client (for ATS APIs + universal URL scraper)
httpx>=0.27.0

# HTML parsing (for universal job URL ingestion)
beautifulsoup4>=4.12.0

# Resume parsing (PDF + DOCX)
pymupdf>=1.24.0            # PDF text extraction
python-docx>=1.1.0         # DOCX text extraction

# Numerical ops (used for embeddings and cosine similarity)
numpy>=1.26.0

# YAML parsing for external config files
pyyaml>=6.0

# Authentication — JWT + bcrypt
passlib[bcrypt]>=1.7.4        # bcrypt password hashing
bcrypt>=4.0.0,<4.1            # passlib 1.7.4 not yet compatible with bcrypt 5.x
python-jose[cryptography]>=3.3.0  # JWT encode/decode
